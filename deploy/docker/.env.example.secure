# AKS Cost Optimizer - Secure Container Environment Variables
# Copy this file to .env and fill in your values

# Azure Service Principal Credentials (required)
AZURE_CLIENT_ID=your-service-principal-client-id
AZURE_CLIENT_SECRET=your-service-principal-client-secret
AZURE_TENANT_ID=your-azure-tenant-id

# Optional: Specific subscription ID (if not set, will discover all accessible)
# AZURE_SUBSCRIPTION_ID=your-subscription-id

# Application Settings
FLASK_ENV=production
PRODUCTION_MODE=true

# Security Settings
FLASK_SECRET_KEY=your-super-secret-key-change-this

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Database Settings (SQLite paths are relative to container)
DATABASE_PATH=/app/infrastructure/persistence/database/

# ML Model Settings
ML_MODEL_PATH=/app/machine_learning/data/models/
ML_TRAINING_ENABLED=true

# Cache Settings
CACHE_TYPE=simple
CACHE_DEFAULT_TIMEOUT=300

# Azure SDK Settings
AZURE_CORE_COLLECT_TELEMETRY=false
AZURE_CONFIG_DIR=/home/appuser/.azure

# Container-specific settings
CONTAINER_VARIANT=bytecode  # bytecode, pyinstaller, obfuscated